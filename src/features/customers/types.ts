export interface Profile {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  residency: string;
}

export interface DocumentItem {
  id: string;
  name: string;
  uploadedAt: string;
}
